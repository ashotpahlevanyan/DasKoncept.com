
	
	var opac = anime({
  targets: '.letter',
	opacity:1,
	delay: function(el, index) {
	  return index * 1000;
	},
});

var opac = anime({
  targets: '.letter2',
	opacity:1,
	delay: function(el, index) {
	  return index * 1200;
	},
});

var opac = anime({
  targets: '.letter3',
	opacity:1,
	delay: function(el, index) {
	  return index * 2300;
	},
});

var opac = anime({
  targets: '.letter4',
	opacity:1,
	delay: function(el, index) {
	  return index * 1200;
	},
});

var opac = anime({
  targets: '.letter5',
	opacity:1,
	delay: function(el, index) {
	  return index * 1900;
	},
});

var opac = anime({
  targets: '.letter6',
	opacity:1,
	delay: function(el, index) {
	  return index * 1700;
	},
});

